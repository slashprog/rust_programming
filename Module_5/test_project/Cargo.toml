[package]
name = "test_project"
version = "0.1.0"
edition = "2024"

[profile.release]
opt-level = 3
debug = false
lto = true
codegen-units = 1
incremental = false
panic = 'abort'
strip = true

[dependencies]
